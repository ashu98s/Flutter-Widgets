import 'package:flutter/material.dart';

void main(){
    runApp(
        MaterialApp(
            home: Scaffold(
                appBar: AppBar(
                    title: Text("Flutter Codes"),
                    backgroudColor: Colors.blueGrey[900],
                ), // AppBAr
            ), // Scaffold
        ), // MaterialApp
    );
}